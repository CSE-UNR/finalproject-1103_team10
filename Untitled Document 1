

#include <stdio.h>

void loadImage(FILE* stream);


int main(){
	}
	
	
void loadImage(FILE* stream){ 
	char fileName[50];
	
	printf("What is the name of the image file?");
	scanf(" %s", filename);
	
	stream = fopen(fileName, "r");
	
	if (stream == NULL) {
	printf("Error opening the file.);
	return 0;
	}
	
